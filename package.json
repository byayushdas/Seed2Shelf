{
  "name": "seed2shelf-monorepo",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install-all": "npm install --prefix frontend && npm install --prefix contracts",
    "compile": "npm run compile --prefix contracts",
    "test-contracts": "npm test --prefix contracts"
  }
}
